import 'package:flutter/material.dart';

class SMS2 extends StatelessWidget {
  const SMS2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SMS2'),
      ),
    );
  }
}
