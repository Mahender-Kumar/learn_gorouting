import 'package:flutter/material.dart';

class SMS3 extends StatelessWidget {
  const SMS3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SMS3'),
      ),
    );
  }
}
