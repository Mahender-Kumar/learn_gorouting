import 'package:flutter/material.dart';

class AddSMS extends StatelessWidget {
  const AddSMS({super.key});

  @override
  Widget build(BuildContext context) {
    print('indsi AddSMS');
    return const Center(
      child: Text('Add SMS'),
    );
  }
}
