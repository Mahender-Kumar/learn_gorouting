import 'package:flutter/material.dart';

class CMS1 extends StatelessWidget {
  const CMS1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CMS1'),
      ),
    );
  }
}
