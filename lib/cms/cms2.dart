import 'package:flutter/material.dart';

class CMS2 extends StatelessWidget {
  const CMS2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CMS2'),
      ),
    );
  }
}
